{
  "name": "bit-maze-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development next dev",
    "build": "NODE_ENV=production next build && node scripts/prepare-hosting.mjs",
    "start": "NODE_ENV=production node dist/server/index.js",
    "test:web": "node --test web-tests/*.test.js"
  },
  "dependencies": {
    "next": "15.5.20",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "overrides": {
    "postcss": "8.5.10"
  }
}
